# Cardinality Estimation Assignment

## Usage
1. Install `uv`.
2. Run `uv sync` to install dependencies.
3. Open and run the experiments notebook:
   ```
   uv run jupyter notebook experiments.ipynb
   ```